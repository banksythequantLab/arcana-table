// ── Arcana Table · config ────────────────────────────────────────────────────
// The DM's brain and voice both live behind this Worker, which holds the API
// key so none of it ever reaches a browser. See worker/README.md.
export const DM_ENDPOINT = 'https://arcana-dm.dj-b02.workers.dev';
